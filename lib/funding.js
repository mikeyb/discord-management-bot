const Funding = {
    getOpenFundings = () => {

    },

    'getFunding': fund => {
        if (typeof fund === 'string') {

            return null;
        }

        if (typeof fund === 'number') {

            return null;
        }
    }
};

module.exports = Funding;